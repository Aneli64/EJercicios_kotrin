package ejercicios_practica_internet

fun main() {
    /*
Se ingresan tres notas de un alumno, si el promedio es mayor o igual a siete
mostrar un mensaje "Promocionado".

    println("Introduzca su primera nota: ")
    var nota1 = readln().toFloat()
    println("Introduzca su segunda nota: ")
    var nota2 = readln().toFloat()
    println("Introduzca su tercera nota: ")
    var nota3 = readln().toFloat()

    var prom = (nota1+nota2+nota3)/3
    if (prom >= 7) println("promocionado") else println("no promocionado")
     */

    /*
Se ingresa por teclado un número entero comprendido entre 1 y 99, mostrar un mensaje indicando si el
número tiene uno o dos dígitos.(Tener en cuenta que condición debe cumplirse para
tener dos dígitos, un número entero)

    //falta la exception en caso de float con un decimal
    println("Introduzca un numero entero: ")
    var numero = readln().toInt()

    if (numero.toString().length > 2) println("Numero no valido")
    else {
        if (numero.toString().length == 2) print("Su numero cuenta con dos cifras")
        else println("Su numero cuenta con una cifra")
    }
     */
}
